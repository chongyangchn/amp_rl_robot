[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "my-amp"
version = "0.0.1"
description = "My own AMP implementation for Unitree G1"
requires-python = ">=3.9"

[tool.setuptools.packages.find]
where = ["."]
include = ["my_amp*"]
